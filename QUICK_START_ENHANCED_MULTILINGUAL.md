# 🚀 增强版多语言检查 - 快速启动指南

## 前置条件

确保以下服务正在运行:

### 1. LanguageTool 服务
```bash
docker run -d --name languagetool -p 8010:8010 erikvl87/languagetool:latest
```

### 2. 后端服务
```bash
cd /Users/anker/anita-project
ANTHROPIC_MODEL='claude-sonnet-4-5' ANTHROPIC_BEDROCK_BASE_URL='https://ai-router.anker-in.com/v1' npm run dev:backend
```

### 3. 前端服务
```bash
cd /Users/anker/anita-project
npm run dev:frontend
```

---

## 🎯 快速测试 (30秒)

### 方式一: 使用测试脚本
```bash
cd /Users/anker/anita-project
./test-enhanced-multilingual.sh
```

### 方式二: 使用 curl
```bash
# 单语言检查
curl -X POST http://localhost:3000/api/v1/enhanced-multilingual/check \
  -H "Content-Type: application/json" \
  -d '{"url":"https://www.anker.com","language":"en-US"}' | jq '.data.textOutput'

# 批量检查
curl -X POST http://localhost:3000/api/v1/enhanced-multilingual/batch-check \
  -H "Content-Type: application/json" \
  -d '{"url":"https://www.anker.com","languages":["en-US","de-DE"]}' | jq '.data.summary'
```

### 方式三: 使用前端界面
1. 打开浏览器访问: `http://localhost:3000`
2. 点击左侧菜单 "多语言文案检查"
3. 输入 URL: `https://www.anker.com`
4. 选择语言: `en-US`
5. ✅ **勾选 "使用增强检查模式"**
6. 点击 "开始检查"

---

## 📖 功能演示

### 输出示例

#### 增强模式输出
```
错误详情

1. "anker" (出现14次)
   原文: Anker | Live Charged.  Anker Prime | Power to...
   修正: Anger | Live Charged.  Anger Prime | Power to...

2. "macximize" (严重)
   原文: ...SHOP NOW Anker Prime Docking Station MACximize Your Creativity...
   修正: ...SHOP NOW Anker Prime Docking Station Maximize Your Creativity...

---
总计发现 19 处语法/拼写错误,其中 "anker" 错误出现了 14 次。
(18 处严重错误、1 处警告)
```

#### 对比标准模式
- **标准模式**: 显示所有 19 个错误的完整详情,包含重复
- **增强模式**: 自动分组为 5 类,突出高频错误

---

## 🎨 界面预览

### 复选框位置
```
┌─────────────────────────────────────┐
│ 网页 URL                            │
│ [https://www.anker.com        ]     │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 选择检查语言                        │
│ [🇺🇸 English] [🇩🇪 German] [🇫🇷 French] │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ ☑ 使用增强检查模式                  │
│   (提供更清晰的错误展示,按错误单词  │
│    分组显示)                         │
└─────────────────────────────────────┘

           [开始检查]
```

### 结果展示
```
┌─────────────────────────────────────┐
│ 检查结果                            │
├─────────────────────────────────────┤
│ 检查语言: 1                         │
│ 总问题数: 19                        │
│ 严重问题: 18                        │
├─────────────────────────────────────┤
│ 🇺🇸 English (US)                    │
│ 文本长度: 0 字符 | 共 0 个问题      │
│ ⚠️ 18 错误  ⚠️ 1 警告              │
│                                     │
│ [展开显示错误详情]                  │
│                                     │
│ ┌───────────────────────────────┐   │
│ │ 总计发现 19 处语法/拼写错误   │   │
│ │ 其中 "anker" 错误出现了 14 次 │   │
│ └───────────────────────────────┘   │
│                                     │
│ ┌───────────────────────────────┐   │
│ │ 错误详情                      │   │
│ │                               │   │
│ │ 1. "anker" (出现14次)         │   │
│ │    原文: Anker | Live...      │   │
│ │    修正: Anger | Live...      │   │
│ │                               │   │
│ │ 2. "macximize" (严重)         │   │
│ │    原文: ...MACximize...      │   │
│ │    修正: ...Maximize...       │   │
│ └───────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

## 🔍 故障排查

### 问题 1: API 返回 500 错误
**原因**: 后端服务未启动或崩溃
**解决**:
```bash
# 检查后端服务状态
curl http://localhost:3000/health

# 重启后端服务
pkill -f "node.*backend"
npm run dev:backend
```

### 问题 2: LanguageTool 服务未启动
**原因**: Docker 容器未运行
**解决**:
```bash
# 检查容器状态
docker ps | grep languagetool

# 启动容器
docker run -d --name languagetool -p 8010:8010 erikvl87/languagetool:latest
```

### 问题 3: 前端显示空白
**原因**: 前端构建失败或服务未启动
**解决**:
```bash
# 重新构建
npm run build

# 重启前端
npm run dev:frontend
```

### 问题 4: 增强模式不显示
**原因**: 未勾选"使用增强检查模式"复选框
**解决**: 确保在提交前勾选该选项

---

## 📊 性能说明

- **单语言检查**: 约 8-12 秒
- **批量检查(2语言)**: 约 15-20 秒
- **批量检查(5语言)**: 约 40-50 秒

*实际时间取决于网页大小和网络状况*

---

## 🎓 最佳实践

1. **选择合适的模式**
   - 初次检查 → 使用增强模式快速了解问题
   - 详细分析 → 切换到标准模式查看完整上下文

2. **批量检查建议**
   - 一次检查 2-3 种语言
   - 避免同时检查过多语言导致超时

3. **URL 选择**
   - 优先检查静态内容页面
   - 避免检查需要登录的页面

---

## 📝 常用命令

```bash
# 启动所有服务
docker start languagetool
npm run dev:backend &
npm run dev:frontend &

# 运行测试
./test-enhanced-multilingual.sh

# 检查服务状态
curl http://localhost:3000/health
curl http://localhost:8010/v2/check

# 查看日志
# 后端日志会在终端输出
# 前端日志在浏览器控制台
```

---

## 🎉 开始使用!

现在你已经准备好使用增强版多语言检查功能了!

**推荐第一步**: 运行测试脚本验证所有功能正常
```bash
./test-enhanced-multilingual.sh
```

**祝使用愉快!** 🚀

---

## 📚 相关文档

- [功能详细说明](./ENHANCED_MULTILINGUAL_FEATURE.md)
- [完整功能总结](./MULTILINGUAL_CHECK_SUMMARY.md)

---

*最后更新: 2025-12-26*
