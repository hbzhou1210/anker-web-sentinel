# 前端生产环境配置
# 用于 Launch 平台部署

# 后端 API 地址
# 请将此地址修改为你的后端服务器地址
# 例如：http://your-server-ip:3000/api/v1
# 或者：https://api.your-domain.com/api/v1
VITE_API_BASE_URL=http://localhost:3000/api/v1

# 注意：
# 1. 后端服务器需要配置 CORS 允许来自 Launch 的请求
# 2. 如果使用 HTTPS，后端也需要配置 HTTPS
# 3. 可以使用 Nginx 反向代理来统一域名
