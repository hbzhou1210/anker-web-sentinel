# 前端生产环境配置
# 用于 Launch 平台部署

# 后端 API 地址
# 生产环境留空,使用相对路径 /api/v1 (通过 Nginx 代理到后端)
# 这样前端和后端可以在同一域名下访问,避免跨域问题
VITE_API_BASE_URL=

# 注意：
# 1. 生产环境前后端通过 Nginx 反向代理统一到同一域名
# 2. 前端访问 /api/v1/* 会被 Nginx 代理到后端 :3000/api/v1/*
# 3. 如果前后端分开部署,才需要配置完整的后端 URL
